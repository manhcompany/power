package com.power.spark.mss

import org.scalatest.FlatSpec

class MatchingSubStrings extends FlatSpec {
  behavior of "MatchingSubStrings"

  it should "MatchingSubStrings" in {
    def matchingSubString[T](input: Seq[T]): Seq[Seq[T]] = {

      Seq[Seq[T]]()
    }
  }
}
